# Canonical development dependency definitions live in pyproject.toml ([project.optional-dependencies].dev).
# Keep this file as a compatibility shim for tooling that expects requirements-dev.txt.
-r requirements.txt
-e .[dev]
